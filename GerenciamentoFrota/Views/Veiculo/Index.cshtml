@model IEnumerable<GerenciamentoFrota.Models.Veiculo>


<p>
	@using (Html.BeginForm("Index", "Veiculo", FormMethod.Get, new { @class = "form-inline" }))
	{
		<div class="col-md-3">
			@Html.TextBox("Pesquisa", "", new { @class = "form-control" })
		</div>
		<input type="submit" value="Pesquisar por chassi" class="btn btn-info" />
		@Html.ActionLink("Adicionar Veículo", "Create", "", new { @class = "btn btn-primary" })

	}
</p>


@Html.Partial("_Veiculos", Model)

